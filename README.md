# 📝 ToDo List Project

## Версия 2.0 🚀 (Текущая)

Полноценное веб-приложение для управления задачами с REST API и HTML-интерфейсом.  
Проект реализован на **Django + Django REST Framework**, с разграничением прав доступа между **администратором** и **обычным пользователем**.

---

## 🔹 Основные возможности (v2.0)

### 👤 Аутентификация и роли
- Регистрация пользователей через сайт
- Вход и выход через Django Authentication
- Роли:
  - **Superuser (администратор)**  
    - Автоматический редирект в Django Admin  
    - Управление всеми пользователями и задачами
  - **Обычный пользователь**
    - Доступ только к своему списку задач
    - Работа через пользовательский веб-интерфейс

---

### 🗂️ Управление задачами (Web + API)
- Создание задач
- Просмотр списка задач (только свои)
- Редактирование задач
- Удаление задач
- HTML-интерфейс без JavaScript (чистый Django Templates)
- Единая логика доступа для API и Web

---

### 🔐 Безопасность
- JWT-аутентификация (access + refresh) для API
- Проверка `request.user` на уровне API и Web
- Администратор видит все задачи
- Пользователь — только свои

---

## 🔹 Используемые технологии

- **Python 3.11+**
- **Django 5 / 6**
- **Django REST Framework**
- **Simple JWT**
- **Django Templates (HTML)**
- **SQLite**
- **Git / GitHub**

---

## 🔹 Архитектура проекта (v2.0)

```text
Client (Browser / REST Client)
        |
        v
Django Views / DRF ViewSets
        |
        v
Authentication
(Django Auth / JWT)
        |
        v
User
        |
        v
Task (привязана к owner)



===========================================================================================================================================================================
# ToDo List API 
**Версия проекта:** 1.0 ✅
REST API для управления списком задач с регистрацией пользователей и JWT-аутентификацией.  
Проект реализован на Django и Django REST Framework, с ограничением доступа к задачам по пользователю.

---

## 🔹 Функционал версии 1.0

- Регистрация пользователей
- JWT-аутентификация (access + refresh токены)
- Защита эндпоинтов по пользователю
- CRUD для задач:
  - создание задачи
  - просмотр списка задач (только свои)
  - редактирование задачи
  - удаление задачи
- Тестирование API через Postman / REST Client / curl

---

## 🔹 Стек технологий

- Python 3.11+
- Django 6.0+
- Django REST Framework
- Simple JWT
- SQLite (для хранения данных)
- Git (для версионирования)

---

## 🔹 Архитектура проекта

- User — стандартная модель Django, хранит данные пользователей
- Task — каждая задача привязана к конкретному пользователю (user)
- JWT — обеспечивает безопасный доступ к данным
- API проверяет request.user, чтобы пользователь видел только свои задачи


          Client (Browser / REST Client)
                  |
                  v
          Django REST API
                  |
                  v
          JWT Auth → User
                  |
                  v
          Task (привязана к user)


          
